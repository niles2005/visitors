(function() {
    mapwork.MapRect = MapRect;

    var EXTEND = null;

    function MapRect() {
        if(EXTEND) {
            EXTEND.apply(this,arguments);
        }
        var lat1 = 
    }

    MapRect.prototype = {
        
    };

    if(EXTEND) {
        mapwork.utils.inherits(MapRect, EXTEND);
    }    
})();